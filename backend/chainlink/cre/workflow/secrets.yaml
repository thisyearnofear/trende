# CRE secret mappings — maps logical IDs to environment variable names.
# In simulation: resolved from local env vars.
# In production: stored in the encrypted Vault DON.
secretsNames:
  VENICE_API_KEY:
    - VENICE_API_KEY
  OPENROUTER_API_KEY:
    - OPENROUTER_API_KEY
